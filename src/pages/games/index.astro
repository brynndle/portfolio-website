---
import { getCollection } from 'astro:content';
import CollectionGrid from '../../components/CollectionGrid.astro';

const entries = await getCollection('games');
---

<CollectionGrid
  title="Games"
  intro="Games I'm currently building."
  basePath="/games"
  entries={entries}
/>
